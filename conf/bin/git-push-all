#!/usr/bin/env bash

# ##############################################################################
#
# ##############################################################################
for i in $(ls -d */)
do
    pushd "${i}"
    cd "${i}" 2>/dev/null
    git add . && git commit -m "updated" && git push
    popd
done
